import { NextResponse } from "next/server";import crypto from "node:crypto";import { requestSchema } from "@/lib/schemas";import { sampleReport } from "@/lib/sample";import { save } from "@/lib/storage";import { planClaim,executePlan,evaluate } from "@/lib/agent";
export const runtime="nodejs";export const maxDuration=300;
export async function POST(req:Request){try{const input=requestSchema.parse(await req.json());if(input.sample||!process.env.OPENAI_API_KEY)return NextResponse.json({...sampleReport,id:crypto.randomUUID(),url:input.url,createdAt:new Date().toISOString()});const id=crypto.randomUUID();await save(id,"input.json",input);const claims=[];for(const claim of input.claims){const plan=await planClaim(input.url,claim,input.instructions);await save(id,`plan-${claims.length+1}.json`,plan);const evidence=await executePlan(id,plan,input.url);const verdict=await evaluate(claim,plan,evidence);claims.push({claim,plan,evidence,verdict});}const score=Math.round(claims.reduce((n,c)=>n+(c.verdict.status==="verified"?100:c.verdict.status==="partial"?50:0),0)/claims.length);const report={id,mode:"live",createdAt:new Date().toISOString(),url:input.url,score,claims};await save(id,"report.json",report);return NextResponse.json(report);}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Verification failed"},{status:400})}}
