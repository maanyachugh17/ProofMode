import {test,expect} from "@playwright/test";
test("demo exposes the intentional product failures",async({page})=>{const errors:string[]=[];page.on("console",m=>{if(m.type()==="error")errors.push(m.text())});await page.goto("/demo-target");await expect(page.getByRole("heading",{name:"Create your Relay account"})).toBeVisible();await page.getByLabel("Email address").fill("not-an-email");await page.getByRole("button",{name:"Create account"}).click();await expect(page.getByRole("status")).toHaveText("Account created");await expect(page.getByText("Invalid email")).toHaveCount(0);await expect(page.getByText("Check your inbox")).toHaveCount(0);expect(errors).toContain("Account audit delivery failed: controlled demo fault")});
test("report keeps JSON export available",async({page})=>{await page.goto("/");await page.getByRole("button",{name:"View sample report"}).click();await expect(page.getByRole("heading",{name:"Verification complete"})).toBeVisible();const download=page.waitForEvent("download");await page.getByRole("button",{name:"Export JSON"}).click();expect((await download).suggestedFilename()).toMatch(/^proofmode-.+\.json$/)});
