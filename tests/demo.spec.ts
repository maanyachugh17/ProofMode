import {test,expect} from "@playwright/test";test("demo accepts invalid email but hides confirmation",async({page})=>{await page.goto("/demo-target");await page.getByLabel("Email address").fill("not-an-email");await page.getByRole("button",{name:"Join the beta"}).click();await expect(page.getByText("Submission received")).toBeAttached();await expect(page.getByText("Submission received")).not.toBeVisible()});
